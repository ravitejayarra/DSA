package com.solid.DIP;

public class DIP {

	
	/*
	 * high level modules should not depend on low level modules , both should depend on abstractions 
	 * 
	 * details should depend upon abstractions , abstractions should not depend on details 
	 * 
	 */
}
